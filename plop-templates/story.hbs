import component from './{{name}}';
import componentHTML from './{{name}}.html';

export default {
	title: '2. {{titleCase type}}|{{titleCase name}}'
};

export const {{pascalCase name}} = () => componentHTML({
	foo: '{{name}}'
});